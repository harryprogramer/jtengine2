package jte2.engine.twilight.spatials;

import org.joml.Vector3f;
import jte2.engine.twilight.models.TexturedModel;
import jte2.engine.twilight.terrain.Terrain;

public class Model extends Spatial {

    public Model(TexturedModel texturedModel, int index, Vector3f vector3f, float scale, Terrain currentWorld) {
        // FIXME    super(texturedModel, index, vector3f, scale, currentWorld);
    }

    public Model(TexturedModel texturedModel, Vector3f vector3f, float scale, Terrain currentWorld) {
        // FIXME super(texturedModel, vector3f, scale, currentWorld);
    }
}
